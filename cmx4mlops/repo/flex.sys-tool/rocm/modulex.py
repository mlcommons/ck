# Author and developer: Grigori Fursin

from cmind import utils

import os
import sys

def process(i):

    cmind = i['cmind']
    output = i['output']
    state = i['state']

    return {'return':0}
